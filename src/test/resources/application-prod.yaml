supplier:
  info:
    protocol: s3
    bucketname: farmfox-stg-bucket
    path: "{0}://{1}/configs/supplierInfo.json"
spring:
  config:
    import: aws-parameterstore:/farmfox-${spring.profiles.active}/